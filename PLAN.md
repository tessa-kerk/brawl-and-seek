# Brawl & Seek — build plan & progress

## ⚡ CURRENT STATE — read this first (updated 13-07-2026 — ✅ SESSION 2 COMPLETE)

*Timeline: M1 built + mobile-fixed 07-07-2026; M1 sign-off, M2 (v5), the projectile Tag (v6) and M3 (v7) all 10-07-2026; PM review + v8 hardening/docs, M4 polish (v9–v11, speed baked 0.70), the TAGGED OUT! fourth end state (v12) and its rename from "SEEKERS EXHAUSTED!" + the Fresh Paint roster lock (v13) all 13-07-2026.*

**WHERE WE ARE: ✅ M1, M2, M3, M4 ALL SIGNED OFF — SESSION 2 IS COMPLETE.** M4 device-verified 13-07-2026 on build **v13** (Tessa: *"The tagged out works perfectly."* — recording at `../Playtests/2026-07-13 - M4 sign-off (v13) - TAGGED OUT verified.mp4`, confirming the TAGGED OUT! reveal, 0/6 found, all-SAFE map, correct leaderboard, seeker health pips + SEEKERS ticker chip mid-round, pace + release-gated scoring behaving, and the immediate end firing at 0:20 right after the last tag was spent). **The whole v13 build — the Event view AND the Map Maker view — is now the frozen regression baseline for the art pass.**

**➡️ NEXT: Session 2b — the ART PASS** (prompt in `../Session Prompts.md`). It swaps real art in behind the `render.js` / `arena.js` seams with **NO game-logic changes**, then Session 3 captures. Everything Session 2 signed off is a regression surface the art session inherits — **re-render approved states and diff before shipping, judge the real frames, keep `?debug=1` working.** Seam notes + draw-order gotchas the art pass must respect are in the **"🎨 SESSION 2b HANDOFF"** block below.

**🎨 ART PASS — v14 REJECTED, MAP RECREATED FROM A REAL REFERENCE, RE-LANDED (build v14 · ART, 18-07-2026) — AT TESSA'S STOP-AND-SHOW GATE.** First pass (same build number, pre-rejection) re-skinned an **invented** layout — a wall/crate ring around the whole border, a square water pool — with painterly texture. **Tessa rejected it on device: "no Brawl Stars map has crates lining the whole outside of the map" / "no water is ever a square."** Root cause: texture quality can't fix a wrong layout. Fix, now Concept Brief §"Art fidelity" rule 3: **the arena RECREATES a section of a real map**, never invents one. Studied via the public map catalogue (`api.brawlapi.com`) — 8 official Paint Brawl maps + 4 Showdown maps, reference-only, no Supercell pixels in the build (Art Inventory.md has the full study + the measured layout laws). Tessa picked **"Spots of Yore"** (an official Paint Brawl map) — `data/arena.js`'s grid now recreates its own true top-left 10×10 corner (tile size measured at 30px, not eyeballed). Consequence, also Tessa's ruling: **bush is a new, REAL 4th camo surface** (not decoration — Brawl's native hiding verb), priority **wall > bush > water > floor**. **Open question, NOT yet answered by Tessa (proceeding on the conservative default, flagged in Art Inventory.md):** does standing in a bush conceal an UNPAINTED brawler like real Brawl, or must every surface including bush require the paint-in first? Currently: paint-in required everywhere, no special-casing.
  - Wired behind the seams: `data/arena.js` — the new grid (no border ring; bush cluster touches the true left edge, exactly like the reference). `src/arena.js` — `isBush`, `camoSurface` priority chain, `hideTiles` derivation extended for bush, `drawWater` now traces a **rounded outer silhouette** per contiguous pool (bounding-box round-rect, not raw per-tile squares — real-map law 3), new `drawBush` (walkable ground-cover texture, procedural fallback). `src/maker.js` + `index.html` + `css/main.css` — a **4th Map Maker toggle** (Bushes), the "no surface camouflages" warning now checks all four. `game.js` — the world-skirt seam was **corrected**: real Brawl never renders anything beyond a map's own bounds, so the original repaint-site diorama (`skirt-1.png`) does **not** ship in the play view; the background is now a dimmed/blurred **continuation of the same floor texture** the arena itself uses. `skirt-1.png` is kept, unused for now — likely serves the landing page (Session 6).
  - **Fankit inventory done** (deep per-brawler coverage but ZERO top-down sprites/tiles/environment — all generated; official full-body renders pulled as fidelity refs, per Tessa's "brawlers stay official" rule).
  - **Regression: 9/9 suites / 80 assertions green** (new `tools/test_bush.py` — 11 independent-oracle assertions: walkability, isolated camo, the full 4-way priority chain at one real grid position with all four surfaces simultaneously in reach, hide-tile inclusion, Map Maker toggle/warning; `test_collision.py` gained an **explicit edge-of-map case set** — 1,120 boundary-adjacent positions × 8 directions, on top of the general sweep which already covered it implicitly; `test_movement.py`/`test_tag.py` repositioned onto the new grid's real geometry — one repurposed as an explicit "map edge stops the player" test, one repurposed as an explicit "real wall cluster stops the player" test). Map Maker overlay + Event fit unchanged; no console errors.
  - Cost: **$1.52 total** (log: `assets/GENERATION-LOG.md`) — floor/water/drum/crate from the rejected pass are **reused as-is** on the new grid (only their positions changed), the only new spend is the bush-cluster asset (2 comps, $0.34).
  - **Brawler sprites are NEXT, after Tessa's device look at this recreated environment** (kickoff step 6 + PM sequence). Generated assets in `assets/world/`; Fankit refs live in the session scratchpad (outside the vault).

**🎨 ART PASS — PROP FIDELITY REDO (build v16 · ART, 18-07-2026) — AT TESSA'S STOP-AND-SHOW GATE.** v15's props (drums, crates, water, floor, bush) were **generic "Brawl-style" objects, not faithful replicas** — Tessa: *"the ones we generated do not look like brawl stars assets… we are trying a faithful recreation."* Fix: pulled Spots of Yore's own map render at native resolution and cropped clean close-ups of its actual wall block, water pool, bush and floor, used as `--ref` for regeneration (same fidelity method as the brawler sprites will use). Result — a real gap closed: the wall block is now a coiled-cap wood crate (not a paint drum), water is flat cartoon blue with a wood rim (not glossy teal), bush is a flat 2-tone scalloped pattern with zero drop-shadow (not 3D spiky foliage that looked like it floated), floor is a two-tone tan checker (restored — v15 had lost it entirely to a flat navy texture). A small Fresh-Paint touch (a paint fleck/drip) kept on each, subordinate to the faithful base.
  - **Consequence, flagged for Tessa's explicit sign-off (not decided silently):** the arena's ground colour is now warm tan/blue, matching the real map — a departure from the locked Night Navy background *in the play view specifically* (UI/chrome stays Night Navy).
  - **Water-pool shape checked precisely** per the PM's "organic scalloped edge" note: at true native-pixel resolution, this specific pool is a clean rounded-square, no scalloping — the already-implemented bounding-box round-rect is the faithful match for *this* pool. Reported precisely rather than inventing a scallop not present in the reference.
  - Code: `arena.js` `drawWallSprites` now draws a single faithful `wall_block` prop (the old drum/crate split retired); the water rim recoloured navy→warm brown to match the real rim; `assets.js` updated. Regression: **9/9 suites, 80 assertions still green** (asset-only change, no grid/mechanics touch). No console errors.
  - Cost: **$2.88 total** (was $1.52) — the 4 new faithful assets cost $1.36, well inside the ~$5–8 envelope (~$5.6 headroom left).
  - Deliverable: a per-prop side-by-side (real map crop vs our replica) for Tessa to judge asset-by-asset, staged via the PM.

**⚙️ ART PASS — CAMERA-TILT DRAW ORDER (build v18 · ART, 18-07-2026) — CANON-DRIVEN CHANGE TO A SIGNED-OFF SURFACE, FLAGGED NOT SILENT.** PM-approved to run in parallel with the map research (below) since it's map-agnostic and needed regardless of which map wins. Real Brawl renders on a tilted camera, not flat top-down (Concept Brief fidelity rule 3d) — chunky blocks show a top face AND a front face, and whether a block sits in front of or behind a character depends on row position, not a fixed draw layer. **Art + draw order ONLY** — collision, all mechanics, and the M3 fit-geometry formula are untouched (re-verified: `test_maker.py`'s independent fit-formula oracle still passes exactly).
  - `arena.js`: walls no longer draw as one blanket pass inside `Arena.draw()`. New `Arena.wallDrawables()` returns one `{y, draw}` entry per wall tile (y = the tile's ground-contact/south edge) plus a flat dark front-face strip drawn beneath each block's own art — a "chunky 3D object" read without needing new front-face art.
  - `game.js render()`: walls + all entities (dummies, seekers, player) now merge into ONE array, sorted ascending by ground-contact Y (each entity's y + r), and draw in that order — a wall correctly occludes what's further from the camera while anything nearer draws over the wall's base. **Proof frame captured** (`Game.pose`-driven, col 8's real 2-tall wall): a dummy positioned behind the wall renders with its lower body genuinely occluded by the block stack (head/name-tag still visible above it); the player positioned past the wall's base renders clearly in front of it, overlapping the block's own shadow/front-face. Real occlusion, not a guessed z-order.
  - **Flagged consequence:** the Map Maker's camo-surface scrim (`drawCamoOverlay`, M3, signed off) can no longer run strictly "over walls, under entities" now that those two are merged — it now runs after the full interleave, so it can lightly tint entities too in the maker sandbox (never in the Event view, which never calls it). Minor, sandbox-only, not silent.
  - **Regression: 9/9 suites, 80 assertions still green** — zero logic touched, only what gets painted where.

**📱 ART PASS — LANDSCAPE-ONLY LOCK (build v19 · ART, 18-07-2026) — CANON-DRIVEN CHANGE TO SIGNED-OFF ACCEPTANCE CRITERIA, FLAGGED NOT SILENT.** Tessa's directive (Concept Brief fidelity rule 3g): Brawl Stars is landscape-only; portrait is unfaithful. **This supersedes the 360px-PORTRAIT acceptance criterion everywhere** (repo `CLAUDE.md` + this doc updated). Started in parallel with the map-pick gate since it's map-agnostic.
  - `manifest.webmanifest`: `"orientation": "any"` → `"landscape"`.
  - New `#rotate-prompt` overlay (`index.html` + `main.css`): a pure-CSS `@media (orientation:portrait)` guard covers the whole screen with a "turn your phone sideways" prompt and hides `#stage` (so no touches reach the game underneath) — no JS needed, so it can never desync from the real device orientation. Verified: fires correctly at a 390×844 portrait viewport, stays fully hidden at 844×390 landscape (screenshots in session record).
  - **Test suite migrated to landscape viewports**, since portrait is no longer a supported acceptance target: `test_touch.py` (was 390×844, now 844×390 — its internal touch coordinates were also fixed, several assumed the old 844px-tall portrait canvas and would have landed off-screen in the new 390px-tall one) and `test_maker.py`'s independent fit-formula check (was `[(1280,800),(390,844),(360,740)]`, now `[(1280,800),(844,390),(740,360)]` — the fit formula itself is orientation-agnostic, only the concrete numbers changed).
  - **Regression: 9/9 suites, 80 assertions green** after the migration.
  - **NOT yet done, explicitly flagged — the bigger half of this directive:** rebuilding the HUD to Brawl's actual landscape layout (Tessa's real screenshot is the reference: joystick bottom-left, attack cluster bottom-right, score chrome top, both floating over the play area rather than in dedicated top/bottom letterbox bands) and the UI-split rule (only Brawl's own control language sits inside the play frame; Map Maker toggles/editor chrome move outside it entirely). Screenshotted the current (still portrait-styled, just orientation-locked) landscape HUD — it visibly doesn't yet match the corner-based real layout. This is real visual-design work, not a mechanical fix, and — same discipline as everything else this pass — deserves its own propose → stop-and-show → iterate cycle rather than a single blind pass. Queued as the next concrete step.

**🔬 ART PASS — DEEP-RESEARCH PHASE (18-07-2026) — NO GENERATION UNTIL TESSA'S NEW MAP PICK.** The v16 fidelity redo (above) was ALSO built from the wrong reference class — the Fandom wiki "map render" image, while a better source than the original thumbnail, wasn't confirmed as true in-game rendering either. Tessa supplied her own genuine gameplay screenshot (filed `../Art/2026-07-18 - real in-game screenshot reference.webp`) and reopened the map pick entirely: rather than keep guessing at Spots of Yore (removed from the game in 2025, footage scarce), recreate a map still in the CURRENT live rotation, faithfully. Full research dossier at `../Research Dossier — Real In-Game References.md` — covers map-render tool fidelity (Pixel Crux: accurate ground colour per theme, but prop icons are generic placeholders, NOT themed art), what BrawlAPI actually exposes (no tile data, ever — only images/metadata; environment name tags can be stale), 3 candidate current-rotation maps (recommended: **Acid Lakes**, Solo/Duo Showdown, "Showdown Mortuary" environment — verified directly, includes the clearest confirmed ORGANIC non-square water shape found all session), the camera-tilt/movement study (brawlers are 3D models, likely smooth-reorient not sprite-flip — flagged for the brawler batch, not fixed now), and the HUD/controls study (Fankit confirmed empty for this need; Tessa's screenshot + Game UI Database's dedicated "Mobile Controls" category are the path). **The grid/mechanics engine (bush surface, 4-way priority, hide-tiles, all 9 test suites) is confirmed map-agnostic — that investment carries forward to whichever map Tessa picks.** Current spend stays $2.88 (the second wiki-referenced batch, $1.02, is honestly logged as spent-but-superseded in GENERATION-LOG.md). Holding for Tessa's map pick + PM's clear before any further generation or the camera-tilt/draw-order engineering.

**🗺️ ART PASS — ACID LAKES GRID DERIVED (build v20 · ART, 18-07-2026) — CANON-DRIVEN CHANGE TO A SIGNED-OFF REGRESSION SURFACE, FLAGGED NOT SILENT.** Tessa picked **Acid Lakes** (Solo/Duo Showdown, "Showdown Mortuary" environment — the deep-research dossier's recommended lead) over the reopened Spots of Yore, then — after the map measured at a genuine **61×61 tiles** (pixel-diff autocorrelation on the wiki's 1952×1952px render, clean 32px tile period, 1952/32=61) against our 10×9 arena — picked **Option A: the top-left corner section** (2 true map edges inherited + organic-continuation treatment on the 2 cut edges) over a full-map-with-follow-camera recreation, per Concept Brief fidelity rule 3h.
- **`data/arena.js`'s grid replaced a third time**, traced tile-by-tile from a 2×-upscaled, gridline-overlaid crop of the real map render (not eyeballed — same discipline as the Acid Lakes measurement itself). **Col0/row0 are the map's own TRUE top-left corner** (the real spiky wall border, no invented ring); **col9 (right) and row8 (bottom) are crop cuts, not real boundaries.** Traced features: the diagonal acid pool wrapping the true corner (touches both true edges, matching the reference exactly); two Power-Cube-spawn floor props at ~col2-3, rows2-4 (walkable floor, not solid — a real Showdown mechanic); a genuine interior wall cluster at row8, cols1-3 (distinct from the true-edge border); a "spawn-pad" wall column at col9, rows0-4 (part of the top border's rightward extent, coincidentally sitting in the cut-edge column). Re-checked against the traced reference post-implementation via a side-by-side render — geometry matches on every feature named above.
- **HONEST GAP, flagged not hidden:** this specific corner has **zero bush/foliage tiles** — the wiki text mentions "bush-covered edges" elsewhere on Acid Lakes, but this section is pool + wall + open floor + two crate props only. The bush mechanic (priority chain, hide-tiles, Map Maker toggle) stays fully implemented and map-agnostic; it simply has nothing to demonstrate on THIS recreated section. A second, smaller honest gap: partial pink decorative props visible at the very bottom-right of the reference crop (outside full legibility, cut by the crop boundary) were left untraced rather than guessed at — deferred to the footage-referenced asset batch, where their solidity/role can be confirmed properly.
- **"Never feel cut off" (rule 3g) — implemented, not just described:** new `Arena.drawCutEdgeFade()` in `src/arena.js`, called at the end of the ground-layer draw pass. A soft linear gradient (transparent → the exact tone the screen-space world-skirt already uses beyond the arena's bounds) fades the last ~1.3 tiles of ONLY the right and bottom edges — "the map keeps going, just past legible focus," not a hard stop. The true top/left edges (the real wall border) are untouched — they're supposed to read as a boundary. Runs on the ground plane only (before walls/entities draw over it in the Y-sorted interleave), so walls and props near the cut edges stay crisp — only open floor/water fades, which is the correct read (we don't want to obscure real recreated content, only signal "more map, unrendered, beyond here"). Verified visually: zoomed crops of the rendered frame show a smooth gradient into the letterbox tone at both cut edges, while the wall cluster at row8 stays sharp.
- **Regression: 9/9 suites, 84 assertions green, zero console errors** — every position-dependent test re-targeted onto the new grid's real geometry (`test_movement.py`, `test_tag.py`, `test_endstates.py`, `test_touch.py`). **`test_bush.py` pivoted its approach**, not just its coordinates: since this map has no bush tiles at all, coupling the bush-mechanic oracle to the live played map would either force fake bush tiles into a faithful recreation (wrong) or make the test vacuous. Instead it now feeds the real, unmodified `src/arena.js` a dedicated synthetic bush fixture via a Playwright network-route interception on `data/arena.js` — same independent hand-written oracle as before, decoupled from whatever any one map's art-directed content happens to contain. This is more correct than map-coupling, not a workaround.
- Cost: unchanged, **$2.88 total** — no new generation this pass (grid + engineering only).
- **NOT yet done, still queued:** the landscape HUD rebuild (flagged in v19, unchanged); the single asset-generation batch (environment props replicated from FOOTAGE of the current Showdown Mortuary skin — not just the wiki render — plus the controls UI + the Belle-Super-referenced Tag icon), held until this grid report clears with the PM.

**📱 ART PASS — LANDSCAPE HUD REBUILD (build v21 · ART, 18-07-2026) — CANON-DRIVEN CHANGE TO SIGNED-OFF HUD SURFACES, FLAGGED NOT SILENT.** PM go-ahead: build to reference directly (Tessa's real screenshot + the general joystick-left/attack-right pattern confirmed via Game UI Database), one PM frame-check, no extra Tessa gate before her device look — this is a replication with unambiguous references, not a design invention. The play frame's control clusters now use Brawl's OWN visual language (Concept Brief rule 3g/3h UI split); our own tools moved out of that real estate.
  - **Bottom-left — a real, visible joystick, not the old drag-anywhere float.** `src/input.js`'s pre-existing `fixed` mode (previously an opt-in debug flag, `?joystick=fixed`) is now the DEFAULT — a fixed base bottom-left matching the proportions in Tessa's screenshot, always visible, no re-anchor. `?joystick=float` keeps the old behaviour as a comparison affordance, not deleted. Restyled from our own teal/yellow brand accent to a neutral translucent grey ring + knob — Brawl's own control colour, not ours (the UI-split rule cuts both ways: OUR chrome keeps our colours outside the frame; BRAWL's chrome inside the frame uses Brawl's, not ours).
  - **Bottom-right — a real Brawl-style circular badge, not a fake attack button.** The old top-centre text-only `#status` chip is now a 64px circular ring-badge in the exact screen slot Brawl's own attack/Super buttons occupy, with a conic-gradient fill driven live by `Player.progress` (paints in as you hold still — magenta while painting, teal glow once camouflaged, dim static when nothing here camouflages you). A hider has no attack, so faking a fire button there would be dishonest; showing the real paint-in state in Brawl's own badge language is the correct read of "occupy Brawl's slot, not Brawl's exact function." `#status-label` (new) keeps the state legible in text above the badge, since the badge is new UI language a first-time player hasn't learned yet.
  - **Map Maker entry moved out of the play frame.** `#mk-open` (was a floating FAB bottom-right, directly competing with the new camo badge for the same corner) now lives in the top chrome rail beside the CONCEPT/build-stamp badges — it's OUR tool, not Brawl's, so rule 3c/3h puts it outside Brawl's own control-cluster real estate. Restyled smaller to fit the rail; icon-only below 480px width.
  - **Score chrome top** — unchanged in position (the ticker was already floating over the play area top-centre); no redesign needed there, this directive was about the two corner clusters.
  - **A real stacking bug caught and fixed en route:** the joystick ring, appended to `document.body` directly, painted OVER the Map Maker phone bottom-sheet (`#makerpanel`, higher z-index) instead of under it — confirmed via `getBoundingClientRect()` + computed-style inspection, not guessed. Root cause: the ring lived outside `#stage`'s subtree, putting its z-index comparison in a different position than the panel's. Fixed by appending the ring into `#stage` instead of `body` — verified by rendered screenshot before/after, not just re-reading the CSS.
  - **NOT yet done, explicitly flagged:** this is the CONTROL LAYOUT and Brawl's own visual LANGUAGE (circular badges, fixed joystick, correct corners) — it is NOT yet Brawl's actual button ART (real icon renders, exact colours/bevel). That pixel-level fidelity is deferred to the footage-referenced asset batch, same as every other prop this pass; today's badges/ring are styled placeholders in the right shape and the right place, not finished art.
  - **Regression: 9/9 suites, 84 assertions, zero console errors** — `test_touch.py`'s joystick-drag margin dropped from a wide margin under the old float-anchor default to 17px under fixed mode (still >2x the 8px pass threshold; re-verified stable after the stacking fix, not a fluke).
  - Visual QA: desktop event + maker, phone landscape (844×390) event + maker, and the narrowest supported width (740×360) all screenshotted and read — badge legible in both idle (dim) and active (filled/glowing) states, joystick correctly occluded by the Map Maker sheet, topbar doesn't overflow at 740px.

**🩹 ART PASS — HUD SWEEP FIXES + SURFACE COUNT HINT (build v22 · ART, 18-07-2026) — PM FRAME-CHECK FOLLOW-UPS.** Two small, concrete fixes from the PM's v20/v21 review notes.
  - **Banner legibility ("SEEKER RELEASED thin on tan"):** same class of bug as the earlier unplated status-chip fix — `#banner` had a text-shadow only, no plate, so it read thin over the world art's varied ground colour. Given the same solid dark plate (`rgba(23,27,51,.72)` + blur) every other HUD readout uses; `game.js`'s `showBanner()` now wraps the headline in a `<span class="main">` for the plate's internal layout.
  - **Per-surface tile-count hint on the Map Maker toggles (PM directive):** each of the four `.mk-surf` buttons now shows a live count ("22 tiles" / "0 tiles") computed once from `Arena.grid` via a new `typeAt()` tally in `src/maker.js` — never hard-coded, so it stays correct on any future map. Zero-count surfaces (bush, on this Acid Lakes corner) render dimmer/italic rather than alarmed — an honest, expected report, not an error state. Verified: 22 wall / 38 floor / 30 water / 0 bush = 90, the full 10×9 grid.
  - **Regression: 9/9 suites, 84 assertions, zero console errors.** Visual QA: both fixes screenshotted and read directly (banner plate legible over tan; count badges correct and correctly styled for the zero case).

**🩹 ART PASS — LANDSCAPE FIT FIX (build v23 · ART, 18-07-2026) — CANON-DRIVEN CHANGE TO THE SIGNED-OFF M3 FIT FORMULA, DELIBERATE AND FLAGGED.** PM frame-check on v21 caught two real defects at 844×390 (and confirmed at 740×360) that a code-only review would have missed.
  - **Event view arena rendered under the chrome.** The old fit formula gave the Event view ZERO top/bottom padding and fit purely against the full viewport. On any landscape-ish aspect (every real device now portrait is blocked), height is the binding constraint, so the arena filled edge-to-edge: top rows rendered UNDER the wordmark/ticker, bottom-row name tags clipped behind the disclaimer ("KIRA" reported half-visible). Root cause confirmed by measuring the actual chrome footprint via `getBoundingClientRect()` (topbar+ticker = a fixed 84px regardless of viewport) rather than guessed. Fix: `game.js resize()` now reserves `padT=92, padB=40` for the Event view too — the same principle Map Maker's own `padT` already used for its top bar. **The M3 independent fit-formula oracle in `test_maker.py` is updated to prove the NEW formula, deliberately — the spec changed, so the oracle proving it changed too, not contorted to keep proving the old one.**
  - **Map Maker's rail/sheet split was gated on raw width, not orientation.** `cssW >= 860` meant a narrow LANDSCAPE phone (844×390) fell under the threshold and got the PORTRAIT bottom sheet — eating ~46% of its height for a postage-stamp arena, exactly wrong given landscape is what gives a device the horizontal room a side rail needs. Fixed in both `game.js` (`cssW > cssH` decides padR-vs-padB) and the CSS (`@media (orientation:landscape)`/`(orientation:portrait)` replacing the old `min-width`/`max-width:859px` pairs, applied consistently to the panel, the topbar right-shift, the camo badge's rail-clearance offset, and the TAGGED! toast centering). Portrait sheet CSS is kept, not deleted, as the honest fallback if the rotate-prompt guard is ever bypassed — it's simply unreachable in real play now.
  - **A side-effect caught by re-screenshotting, not assumed away:** fixing the rail breakpoint meant the 340px rail now claims space at EVERY landscape width, not just ≥860px — at 740px that left the wordmark too little room and it wrapped into the CONCEPT badge. Fixed with a targeted `body.maker` font-size reduction below 800px landscape.
  - **Regression: 9/9 suites, 84 assertions, zero console errors.** Visual QA: re-screenshotted event + maker at both 844×390 and 740×360 with the banner clear of the frame specifically to inspect the top/bottom edges directly — arena content now visibly clears the chrome and the disclaimer at both sizes, the rail renders correctly at both sizes, the wordmark no longer wraps.
  - **Also closed, carried forward from the same review pass:** the SEEKER RELEASED/HIDE! banner plate (already shipped at v22, before this note arrived — confirmed still correct in this pass's screenshots, no further action).

**🎨 ART PASS — ACID LAKES ENVIRONMENT BATCH (build v24 · ART, 18-07-2026) — PM-CLEARED GENERATION, WIRED.** The single batch estimate ($2.72–$3.06 projected) was approved as submitted; generated, self-QC'd, cut out, and wired in this pass.
  - **Wired:** `floor.png`, `water.png`, `wall_block.png` (all Acid Lakes-faithful, replacing the Spots of Yore set), a NEW `powercube.png` prop + a NEW `Arena.drawProps()` decal system (`data/arena.js` gained a `props` array; two traced Power-Cube spawn markers at col3/row2 and col2/row3 — flat ground decals, never solid, never consulted by collision/hide-tile logic), and two UI icons (`assets/ui/tag_icon.png`, `assets/ui/camo_icon.png`) — the camo badge's 🖌 emoji placeholder is now the real generated glyph (non-breaking swap, falls back to the emoji on any load failure).
  - **Reference sourcing, stated honestly (not overclaimed as footage):** genuine current-skin "Showdown Mortuary" gameplay VIDEO was searched for directly and not found within reasonable effort (two focused YouTube passes). Used instead: the wiki's own verified-direct `Acid_Lakes-Map.png` (the same render the grid itself was traced from) for shape/base-hue, and the wiki's own verified-direct `Showdown_Mortuary-Environment.png` mood banner (dark graveyard, glowing acid-green) for the current skin's actual atmosphere — blended in the generation prompts. One real gameplay frame (an OLDER, non-Mortuary skin) was used only to confirm the 3D camera-tilt/chunky-render STYLE, never colour. Full sourcing detail in `GENERATION-LOG.md`.
  - **A real cutout bug caught and fixed mid-batch, not shipped:** the wall block's requested "acid-green glow" accent and the Power Cube prop's green gem icon each keyed out against their own green-screen background (the exact naive-colour-key failure the image-editing skill's own docs warn about — asking for green content ON a green screen). Caught by compositing every cutout over mid-grey before use (the skill's own stated check), not skipped. Fixed by regenerating both on magenta instead (new `magentakey.py` cutout script, mirroring the project's existing `bluekey.py` pattern) — verified fringe-free on the second pass.
  - **Organic water silhouette, implemented as scoped free engineering:** `drawWater()` no longer fills one bounding-rect per pool region — it now unions a rounded-rect per water TILE (same technique `drawBush` already used), so the fill traces the real diagonal stair-step pool shape instead of a rectangle. The terracotta rim is now a `shadowBlur`-cast glow off that same per-tile union rather than a per-region stroke — canvas shadows read the final composited alpha, not individual subpaths, so adjacent water tiles never show a seam where they touch. A minor, honestly-noted texture nuance: at diagonal-only tile junctions a very small rim-coloured accent can show through; tuned `shadowBlur` down to minimise it, judged acceptable on the rendered frame rather than chased to zero.
  - **Two estimate line-items deliberately not spent, not silently dropped:** (a) the joystick ring/knob texture — re-checked Tessa's own reference screenshot first; real Brawl's joystick is a plain, untextured translucent shape, so adding painted art there would be a fidelity REGRESSION, not an improvement; the existing CSS ring already matches. (b) the contingent bottom-right partial props — stayed genuinely gated on footage confirmation as scoped in the estimate; none was found, so honestly skipped, no charge.
  - **Queued, not rushed:** the Tag hit-mark/target-marker overlay (the in-flight projectile-impact visual) — the icon itself is done and wired, but it isn't displayed anywhere in the UI yet (no seeker-side HUD surface currently shows it) and the actual hit-mark animation is real remaining scope, flagged here rather than half-built into this pass.
  - **Cost: $6.79 of the $8 ceiling** ($1.21 headroom) — includes the two superseded fringe-fix regenerations, honestly logged, not hidden.
  - **Regression: 9/9 suites, 84 assertions, zero console errors.** Visual QA: rendered frames re-screenshotted at desktop and phone landscape, both Event and Map Maker views, plus a zoomed crop confirming both Power-Cube props render at the correct scale/position; side-by-side comparisons built against the reference crops for every environment asset.

**🩹 ART PASS — BANNER RIGHT-SIZE (build v25 · ART, 18-07-2026) — PM FRAME-CHECK FOLLOW-UP, STAGING-READY.** PM's v23 frame check found the SEEKER RELEASED/HIDE! banner plate — plated at v22 to fix a legibility bug — now oversized relative to the fitted arena at 844×390, covering most of the play area while showing. Not a defect (it's transient), but flagged to right-size against how Brawl scales its own event callouts. Fixed: `#banner .main` font-size clamp cut from `clamp(26px,7vw,52px)` to `clamp(18px,4.4vw,30px)`, padding and the subtitle trimmed to match, plus a short-landscape-specific further reduction (`max-height:430px`) for the exact 844×390/740×360 case the PM's frame flagged. Re-screenshotted both sizes: the banner now reads as a HUD callout, not a takeover — hiders, both Power-Cube props, and much more of the arena stay visible around it mid-banner. Regression: 9/9 suites, 84 assertions, zero console errors.
  - **This closes every open PM note on the Acid Lakes environment batch (v24).** Staging package for the PM's full frame check (desktop, 844×390, 740×360, event + maker, plus a posed paint-in frame) is ready: generation log (`GENERATION-LOG.md`), per-asset side-by-sides vs. the reference crops, the wired build at v25, full suite green.

**🛑 ART PASS — v24/v25 ENVIRONMENT REJECTED ON DEVICE, ROOT CAUSE CONFIRMED (18-07-2026, Concept Brief rule 3i).** Tessa: **"the art does not look like brawl stars at all."** The fourth rejected art round this session. Her own search found real gameplay footage (https://www.youtube.com/watch?v=A0lq8d8TUwk) a prior "reasonable effort" research pass missed. Fact-checked her hypothesis directly rather than taking it on faith: downloaded the video (`yt-dlp`), extracted 26 frames across the full runtime (`OpenCV`), compared against `Acid_Lakes-Map.png` (the wiki render the whole v24 batch was built from). **CONFIRMED — the two classes match on nothing:** wiki render shows a flat violet checker + teal spiky walls + acid-green pools + graveyard mood; real gameplay shows warm orange sand, brick/wood cover clusters, sky-blue organic pools, genuine bush clusters, wooden fences/barrels, bone decor, and a canyon-cliff map boundary — a completely different art direction for the same map. Full evidence table + side-by-side composite in `Art Inventory.md` (§"REFERENCE-CLASS CORRECTION #3"); reference frames persisted to `Art/Acid Lakes Real Footage Reference/`.
  - **New canon, rule 3i:** a wiki/Fandom map-render image is layout-only, never a rendering-style reference. Art research that hasn't watched gameplay video does not satisfy the reference requirement, regardless of how official a static wiki asset looks.
  - **What survives, what doesn't:** the corner's traced TILE POSITIONS (wall border, diagonal pool touching two true edges, prop placement) are provisionally still usable as geometric fact, independent of render style. Every generated ASSET (wall/floor/water palette + material, the Mortuary mood) is fully superseded and needs a video-referenced redo.
  - **PM's parallel work order, all free/research (no generation):** (1) fact-check ✅ done, confirmed; (2) video-frame reference pack ✅ done; (3) a widescreen crop proposal (Tessa: "square can be played portrait kills the fiction" — the play area must extend side-to-side like Brawl); (4) a Fankit backdrop re-check — attempted, blocked by the Fankit SPA's own error boundary across three tries, genuinely blocked not skipped, flagged for a follow-up (likely needs a human-logged-in session).
  - **HOLD, explicit, not started:** any generation (environment redo or the brawler batch) — waiting on the PM's relay of Tessa's budget call ($8 ceiling vs. extending to ~$12 to cover both the environment redo and the 7-brawler batch). The research/fact-check/frame-extraction/crop-proposal work is unblocked and free; only pixels wait.

**🔧 ART PASS — THE TWO-SURFACE SPLIT, FREE-ENGINEERING CHECKPOINT (build v26 · ART, 20-07-2026, Concept Brief rule 3j).** Tessa recorded her own footage (`Acid Lakes.MP4` + `Brawl Stars Map Maker.MP4`, ledger H7, ingested and released this pass) and made a structural call: the prototype had been combining two separate concepts — the in-game event and the Map Maker toolkit — into a third thing that exists in neither surface. Five rulings, all actioned this pass except generation (still held):
  - **Public prototype = the event, pure.** `#mk-open` (Map Maker entry) now hidden by default, revealed only behind `?debug=1`/`?maker=1` (same established pattern as the pace picker) — not deleted, fully functional and tested (`test_touch.py` updated to pass `?debug=1`). The wordmark is gone from the play surface entirely; the CONCEPT badge + build stamp (a hard line — must never be hidden, in any view) shrink into a plain corner row instead of a full title bar.
  - **Chip-row ticker → in-game overlays.** The old 6-chip ticker (SCORE/rate-coin/REPAINT/HIDERS/SEEKERS/TIME) is replaced by two sparse corner plates — `#ov-left` (hiders/seekers, learned directly from her recording's "Brawlers left: N" position/plate/type language) and `#ov-right` (score/time — our mechanic's own feedback, since Brawl has no scoring analogue to borrow, given the same plate language rather than invented chrome). REPAINT time and the camp-decay coin dropped from persistent display — already conveyed by the camo badge's own live fill. A real overlap bug (the score plate covering the build stamp) was caught by screenshot and fixed before shipping.
  - **Movement speed re-derived by measurement, not A/B.** Dense-sampled her bot-match recording (0.15s intervals, OpenCV) across a clean walking segment, measured world-scroll speed via `cv2.phaseCorrelate` (independent of our own movement code — never calls `Input`/`Player` logic), summed as path length (not net displacement, since the walk curves) over 2.4s: ≈101px/s. Tile size measured the same way from a wall block's on-screen width: ≈42px/tile. **2.4 tiles/s** — independently cross-validated against the community unit-conversion estimate for Brawl's "Normal" speed class (720u ÷ ~300u/t/s ≈ 2.4 t/s), the two methods agreeing. New `speedScale = 0.57` (was the blind-A/B 0.70) — confirms Tessa's "way too fast" call was right by ~19%. Full writeup in `data/tuning.js`; `test_speed.py`'s oracle updated deliberately to 0.57.
  - **Widescreen crop, PM-cleared: 16×9 (was 10×9).** "A square arena can be played portrait — it breaks the fiction." The original 10 columns are UNCHANGED (their layout was already fact-checked against the real map render and is provisionally still trustworthy for tile positions, even though that render's rendering STYLE was demoted); 6 new columns appended along the same true top edge. **Honest scoping note, on record:** her footage doesn't include a clean establishing shot of this exact map edge extending further right (it's mid-match gameplay, not a map-select fly-through), so the 6 new columns are a footage-INFORMED, pattern-matched extension (a bush cluster, a second small organic pool, a wall-block cluster — all features her footage confirms genuinely exist on this map) rather than a literal pixel trace. Flagged for the PM's clear like everything else this pass. Bush gap resolved: the original corner's honest "0 bush tiles" gap now has 3 real bush tiles (cols10-12, row2) — her footage confirms bushes ARE a real feature (teal foliage, not the green the wiki-render era assumed). Dummies **5→7** (PM-approved, ~60% more floor). Procedural fallback palette nudged toward her footage's actual current theme (dark violet ground, teal foliage, glowing acid-green water) — real textures redo against her frames in the held batch.
  - **Mechanics re-verified, not assumed:** `test_canon.py`'s repaint-escalation oracle needed re-deriving (the "≤2 hiders alive" threshold — which INCLUDES the still-live player per `round.js` — shifted from found≥4 to found≥6 against the larger 7-dummy roster; independent oracle recomputed from the spec, not copied from the implementation). Collision/edge-of-map sweeps re-ran automatically (grid-agnostic) at 14,312 and 4,152 cases respectively on the wider grid. Round-resolves-under-90s re-verified at the new 0.57 scale (36.4s, comfortably inside).
  - **Map Maker UI frame inventory** (for the later parked deliverable — a standalone visual mockup of the real Map Maker modifier screen with "Camouflage" as a modifier, NOT built into the prototype): 4 clean reference frames filed to `Art/Acid Lakes Real Footage Reference/`, including one showing the real "Modifiers" button — the exact surface that later mockup targets.
  - **Regression: 9/9 suites, 84 assertions, zero console errors.**
  - **HELD, not started:** any generation — the environment redo (against her footage, not the demoted wiki render) and the 7-brawler procedural-animation batch. Waiting on the PM's clear + Tessa's $12 budget yes, per the sequencing she set.

**✅ M4 REOPEN — TAGGED OUT! end state SHIPPED & SIGNED OFF (logic build v12, renamed v13; device-verified 13-07-2026; Concept Brief v3.4).** *The canon-driven changes this reopen made to the signed-off M2 surfaces — the SEEKERS ticker chip, the seeker mistake-pips/health bar, and the four-state reveal copy — are now themselves **signed off** as part of v13. No open flags remain.* Tessa's M4 pace test found a real hole: she kited **both** seekers into spending their whole tag budget (four wrong tags each → spectator), so **every seeker benched and the round dead-aired to the timeout with zero threat on the map** — yet score kept ticking, camp decay kept eating her rate, and every reposition banked a risk-free +100. `round.js` only ended on `spotted | timeout | all-found`; nothing checked for all-seekers-out. **Fix (canon, now in `round.js`):** a round now ends **four** ways — the new one is **all seekers spectating AND no tag in flight → round ends immediately, hiders win, "TAGGED OUT!"** (the mirror of SPOTTED!, same stamp family — the hiders turn the Tag mechanic back on the seekers; the clip where they outlast the tag budget). *(Renamed from "SEEKERS EXHAUSTED!" at Tessa's call, v13 — "not Brawl enough"; TAGGED OUT! pairs with SPOTTED! in the locked motif language. Internal end-reason id is `'tagged-out'`.)* **In-flight nuance is canon and implemented:** the end check runs **after** tag resolution, not on the bench event — a tag airborne when the last seeker benches **resolves first**; if it hits, the found hider converts (fresh 100 health, also now canon) and the round **continues**; if it misses → TAGGED OUT!. *(The pace-A gate and the scoring gate both visibly PASSED in that recording — score held 0 through the hide phase.)*
- **✅ Edge-case sweep shipped as `tools/test_endstates.py` (9 assertions, independent oracle = the spec):** TAGGED OUT! ends & hiders win; in-flight tag delays the end then a miss → tagged-out; an in-flight tag that hits converts a hider → fresh seeker → round continues; no tag fires during the hide phase; converted seekers arrive fresh 100hp/0 mistakes; simultaneity (player hit by the same tag that would bench the last seeker → **find wins**, no bench); score-farm audit (no score on the ending frame); dummy leaderboard rows freeze at round end. Rounds still resolve <90s across all four end states at pace 0.70 (`test_speed.py`). All green, stable across repeated runs.
- **✅ Seeker-health legibility (UI — canon-driven change to the signed-off M2 ticker/HUD, flagged):** the seekers' dwindling tag budget was invisible until they vanished, so the new ending would look random. Added a **SEEKERS-remaining chip** to the ticker (turns magenta when ≤1 in the seek phase) and a **mistake-pip readout + health bar** on each active seeker. Verified at phone size mid-chase (pips read ●●○ at 390px). The 6-chip ticker fits every phone width — full labels ≥390px, a compact glyph fallback (`🖌 · H · S`) ≤380px; measured last-chip right 364px@390, 313px@360.
- **✅ Reveal copy per end state (additive copy on the signed-off reveal surface, flagged):** the reveal panel now says what happened in all four cases (SPOTTED! / ALL FOUND / YOU SURVIVED on the clock / TAGGED OUT!), each with its own stamp — the new teal-on-ink **TAGGED OUT!** stamp mirrors the magenta SPOTTED! one (both drawn by the generalised `drawStamp`; the stamp family stays full-size, only a >11-char label would shrink).
- **Not bugs (noted, do NOT change):** player-found-ends-the-round (dummies' fates don't play out) is deliberate demo scope; the last seeker has no "desperation" behaviour — a balance idea, not for now.

**🎨 SESSION 2b HANDOFF — seam notes the art pass MUST respect (art swaps behind the seams; NO game-logic changes).** The renderer is deliberately swap-friendly, but the paint-in mechanic and the fit geometry make some seams sharp. Read all of this before generating or wiring art. The art session reads THIS doc, not the build session's memory.
- **Don't touch these signatures / call sites — only their insides:** `render.js` `drawNormal(ctx,cx,cy,r,facing,col,opts)` and `drawCamo(ctx,cx,cy,r,color,extraAlpha)`; `arena.js` `draw()` (`drawWater`/`drawWalls`/floor checker); the **background fill in `game.js render()`** (the flat `#171B33` letterbox — this is the **world-skirt** seam). Game logic, the paint-fill compositor, and all state stay put.
- **The paint-fill is a live clip, not two static images — this is the trap.** The one-second fill composites by clipping `drawNormal` (shown right of the moving wet edge `sweepX`) against `drawCamo` (shown left) across the sweep. So a sprite dropped into `drawNormal` gets **clipped mid-body every frame** during a paint-in. Two hard requirements: **(1)** the **hidden silhouette in `drawCamo` must share the exact outline** of the `drawNormal` sprite — if they differ, the paint-in reads as a *morph* between two shapes, not one character painting in. **(2)** the hidden state is a **flat, surface-TINTABLE silhouette** (an alpha mask the renderer tints to wall/floor/water at `camoAlpha` ≥ 0.24) — **not** the full-colour sprite at low alpha. This tintable silhouette is the money asset; author it as a mask, not a picture.
- **Keep procedural (motion, not art):** the sweep + magenta wet edge, the proximity ripple, the you-are-here pulse, break-bursts, the tag projectile + tracer, health bars, name tags, mistake-pips, both end-stamps. Do **not** replace these with static art or add art-driven motion that ignores `STATE.reduceMotion` (the reduced-motion pass is signed off).
- **`e.col` is identity, not just body fill.** `drawNormal` currently fills the body with `col`, but `col` is **also** used for the reveal-map leaderboard dots, name-tag accents and the you-are-here ring. A full-colour bitmap sprite carries its own colour — so decide whether `col` becomes an outline/accent tint or a no-op on the body, but **keep a representative `col` per brawler** or the reveal card and dots lose their per-hider colour.
- **Facing flips horizontally** (`e.facing = ±1` → `ctx.scale(-1,1)`). Author each sprite facing one way; asymmetric details (Belle's gun) flip with it — fine. Don't bake text/logos into a sprite.
- **Draw order (under → over):** world skirt (screen space, beneath everything) → arena tiles → entities → FX/tracer → screen-space stamps/HUD. The skirt fills the letterbox **outside** the fit-contained arena and must sit **under** the arena, never over it. Name tags / health bars / pips draw **above** the head — leave headroom in the sprite art so they don't collide.
- **The fit geometry is a frozen regression surface.** The Event-view fit (`Game.scale`/`off`, matching the pre-M3 formula exactly, proven in `tools/test_maker.py`) must **not** change for art — art fills *within* it. Entities and tiles are world-coords transformed by that fit; the skirt is the only screen-space background layer.
- **Seeker `spectator` state is renderer-ghosted** (low alpha / desaturated) — supply one normal seeker sprite and let the renderer ghost it; don't author a separate ghost sprite.
- **Authoring size + retina:** captures run at `device_scale_factor=2`; author ≥3× (character world box r≈22 → ~128×128 transparent PNG; tiles ≥128×128 for a 64px world tile; skirt full-bleed ≥2560×1600). Full asset list, states and the Fankit-vs-generate verdicts are in `../Art Inventory.md`.
- **Non-negotiables that survive the art pass:** the `CONCEPT` label + the *"unofficial / not endorsed by Supercell / Fan Content Policy"* notice on **every** build and capture; `?debug=1` keeps working; re-render each signed-off state and **diff before shipping** — flag any visual change to an approved surface, never ship it silently.

**M4 — the brief (Tessa's M3 sign-off feedback + the scoped polish):**
1. **SPEED (the gated item — her recording judges it).** *"The speeds just feel really fast for everyone."* This is **global movement feel, not seeker-vs-player balance** — the validated ratios (player 4.2 vs seeker 3.7 so a runner breaks away; juke beats lead; tag 2× base) are **PRESERVED**; only the absolute values come down. Approach per instrument-don't-guess: a **global `?speed=` multiplier** applied uniformly to player + seekers + dummies + tag (ratios hold), with 2–3 candidates for Tessa to A/B on device. Her pick bakes into `TUNING.speedScale` with a dated note; the param stays as a debug affordance. After baking: re-run canon (the 2× tag assertion survives a uniform scale) and sanity-check the round still resolves well inside 90s and camping still gets punished.
2. **SCALE — no action.** Tessa accepts the compact arena; the scaled-demo framing (Concept Brief v3.2 §106) covers it.
3. **ART — bring the pass forward, don't reassure in words.** Her worry: *"hard to tell without the actual Brawl Stars assets… I just hope the concepts translate once we put in all the assets."* Two M4 asks: **(a)** confirm nothing in the polish makes the sprite/tile swap harder (`drawBrawler` + arena tiles stay cleanly replaceable); **(b)** deliver the **ASSET INVENTORY** at the M4 gate — the exact list of visual assets the prototype needs, each marked *likely Fankit* vs *must generate*, per the locked BrawlHouse workflow (Fankit first, unmodified; generate fan art of actual brawlers only where the kit falls short). The inventory is the **handoff doc** for a separate art session under Tessa's image-gen cost gates.

Plus the scoped M4: **mobile-controls polish, full `prefers-reduced-motion` pass, performance pass.** Same gate: stop and show; her device recording judges the speed feel.

*Recording note: Tessa's M3 device recording is filed at `Projects/Brawl & Seek/Playtests/2026-07-13 - M3 Map Maker sign-off (mobile, v8).mp4` (per the Project Brief).*

**M4 BUILT (build v9) — awaiting Tessa's speed A/B on device:**
- **✅ GLOBAL SPEED — BAKED at `TUNING.speedScale = 0.70` (v11, Tessa's device pick, and it's BRAWL-TRUE).** She blind-picked **A** (*"it feels more like the actual speed brawlers move at in-game"*), then asked if we could match real Brawl movement — and research (Concept Brief §Design tuning, 13-07; Pixel Crux roster data) says A already does: Brawl classes Normal 720u / Fast 770u / Very Fast 820u. At 0.70 our **player ≈ 2.94 t/s (≈ Very Fast — Leon's class**, a neat "why not just Leon" foil: hiders literally move at his speed) and the **seeker ≈ 2.59 t/s (≈ Normal)**. Conversion-independent check: our hider:seeker ratio **0.881 ≈ Brawl's 720/820 = 0.878.** The scale multiplies **every** mover uniformly so the validated ratios hold exactly (tag still 2.00× base). *(The unit→t/s conversion is community-disputed — 300 vs a measured 273.33 — so the absolute t/s carry a band; the comment says so; do not "correct" them.)* Re-verified at 0.70: full round resolves ~45s (well inside 90s); idle-camp find window **21–30s** (still punishes camping); all canon assertions green.
- **⚙️ Pace picker DEMOTED to a debug/capture affordance (`src/pace.js`).** It did its job (let Tessa A/B in the URL-bar-less PWA). Now that A is baked, the **default Event view shows no picker**; it appears only with **`?debug=1` or `?pace=1`** (kept for Session-3 capture A/B). `?speed=<n>` + `Game.setSpeed` also stay. One file + one DOM block to delete entirely if ever wanted.
- **`prefers-reduced-motion` full pass.** Decorative motion off under reduce (the +100 bounce → opacity-only `bonusfade`; the TAGGED! toast keeps the brand tilt but drops the scale-pop; switch knob snaps). Kept: opacity fades (not "motion") and **the paint fill itself** (it's the mechanic; its wobble/drips + FX bursts/ripples were already gated). Verified: `STATE.reduceMotion` honoured, `#bonus` uses `bonusfade`, no errors.
- **Performance pass.** Phone viewport, live seek phase with movement + tags: **59–60 fps free-run, 60 fps under 4× CPU throttle, 60 fps worst-case (7 seekers + tags)**. The build is light (canvas + ~15 entities); no jank. Landscape phone also renders and fits.
- **ART (a) — swap-friendly confirmed.** All character art flows through `render.js` `drawNormal`/`drawCamo` (clean `ctx,x,y,r,facing,col` signature) and all tiles through `arena.js draw()`; the paint fill composites those two by clipping across the sweep, so **sprites drop in without touching game logic.** M4's speed + reduced-motion changes don't go near these seams.
- **ART (b) — ASSET INVENTORY delivered:** `../Art Inventory.md`. **Expanded 13-07 after Tessa raised the bar** (Project Brief decision 9: *"look truly polished, like an actual Brawl Stars map just on a smaller scale… ensure it is perfect"*). Now covers the **full picture**, not just sprites: the **world skirt outside the play bounds** (the map currently floats on void — the single biggest "reads as Brawl Stars" win), themed **Fresh-Paint** wall props (crates/barrels/drums), textured ground, a proper water treatment, decor, plus the brawler state sprites, the wordmark/stamp, and a UI-weight re-judge. Each marked *check Fankit → almost certainly generate* (honest: the Fankit has no top-down map/sprite art, so the generate column is long). Handoff for a separate art session under Tessa's image-gen cost gates. **The art pass lands before Session 3 captures.** The same map sits in an **editor frame** with three **live** properties, applied with no restart:
- **Camo surfaces** (walls / floor / water) — `Arena.camoSurface()` now reads `STATE.camoSurfaces` and returns **null** when nothing qualifies: switch a surface off and standing there never paints you in (the status chip says *"No camo surface here"*). Turn all three off and the panel warns that nobody can hide.
- **Repaint time** (0.5s / 1s / 2s) — a real segmented control; the next paint obeys it immediately.
- **Ripple tell** on/off — a real switch. Off means no ripple *and* no clue: with the tell off, a bot with its notice chance cranked to 5/s **never once** locked onto a hidden hider (verified). Camouflage becomes perfect against them.

The **arena itself shows the property**: enabled surfaces glow teal, disabled ones fall under a scrim, so flipping a toggle changes the map in front of you.

> **Known characteristic (working as intended), for Tessa's "did the toggle change the map?" question.** The three toggles differ in felt strength. Measured on the live build: with **Floor off, only ~8% of standable positions** become no-camo (the open centre around tile 5,2) — because `camoSurface()`'s 0.42-tile hug-reach means a wall or water still paints you in almost everywhere on a compact 10×9. So **Floor is the subtlest toggle** and the scrim overlay carries most of its felt change; **Walls, Water and the Repaint/Tell toggles read far more strongly** in play. This is honest to the mechanic (hugging reaches nearby surfaces) — not a bug. If Tessa wants Floor to bite harder on this map, the lever is the hug-reach or a larger/opener arena (M4-adjacent), *not* the toggle. It's a **test-play sandbox** (`src/maker.js`) — no round clock, no score, no dummies; one bot seeker patrols so the tell and the Tag stay meaningful, and being tagged just respawns you with a `TAGGED!` stamp. Editor panel docks as a right rail ≥860px and a bottom sheet on phones. Enter via the floating **Map Maker** button, leave via **◂ Event**; the Event view then resets to canon (all three surfaces, tell on).

**🐛 A REAL BUG THIS FOUND — and it was breaking M2 too.** `input.js` called `preventDefault()` on every `touchstart` inside `#stage`, which stops the browser synthesising a click. **Every UI button was dead on a phone** — the Map Maker button, the panel controls, and **M2's "Play again"**. Fixed: touches beginning on interactive chrome (`button, a, #makerpanel, #endpanel`) are excluded from the thumb-stick and never `preventDefault`-ed, with `touch-action` re-enabled on those surfaces so the panel scrolls. Verified by real taps: buttons fire, the joystick still drives on the arena. *(This is a fix to a signed-off surface — flagged, not silent.)*
  - **v8 hardening (13-07, from the PM review):** the UI-touch set was add-only and only cleared on `touchend`. If iOS drops a UI `touchend` and reuses the identifier (the same ghost class as the M1 saga), a stale id could exclude a later game touch from the stick forever. Now membership is **reconciled per `touchstart`** (set/clear by `isUI` for each changed touch), which self-heals instantly. Covered by `tools/test_touch.py` (dropped-UI-touchend + id-reuse → still drives).

**🧹 Removed a lying debug helper.** `Game.setRepaint()` had been dead since M2 — the event loop overwrites `STATE.repaintTime` from the round's escalation every frame (measured: set 0.5 → back to 1.0 one frame later). Repaint is owned by **`Round.repaintTime()`** in the event and **`MAKER.repaint`** in the editor. Deleted, and the stale test rewritten to exercise the real path.

**REGRESSION PROOF (Event view is signed off, so it must not move):** the fit geometry matches the pre-M3 formula **exactly** at 1280×800, 390×844 and 360×740 (independent oracle: the original formula recomputed in the test); ticker, hint and chrome intact; **13/13 tag + 17/17 round canon assertions**; the geometric collision proof still **0 clips / 0 stuck** across 5,968 cases; M1 movement, paint timer and camo break all green. Zero console errors.

**✅ SCORING STARTS AT SEEKER RELEASE — canon, implemented (Concept Brief v3.3, 13-07-2026).** The hide-phase-scoring question I flagged is answered: **hider score, the camp-decay clock and the ≥3-tile reposition bank ALL start at seeker release.** The 15s hide phase is unscored setup — nothing accrues, decays or banks before the release banner. Painting in during hide is still allowed and encouraged; it just doesn't earn until the seeker looses; first-spot decay counts from release. **This changes the ticker on the signed-off M2 surface — canon-driven (v3.3), flagged not silent:** pre-release the ticker now reads **score 0, coin at full strength** (the recording's "116 pts / +9.1/s six seconds into the hide phase" state is no longer reachable). Dummies obey the same rule. *(Rationale: prep scoring rewarded painting in instantly at spawn and punished using the phase to find a good spot; decay before any threat is noise that teaches the mechanic wrong; and the ticker moving on the SEEKER RELEASED beat is better clip logic — points are only ever earned in danger. Precedent: Meccha doesn't score its prep phase either. Note: Meccha has NO camp decay — its score is distance-to-seeker + line-of-sight time; our decay/reposition model is deliberately our own, do not "fix" toward Meccha.)* Proven by an independent oracle in `tools/test_canon.py` (no score/decay/bank in hide; accrues + banks at release). **Max survival score in the compressed demo drops** (~15s of prep points removed) — no assertion hard-codes a total, so nothing else shifted.

**⛔ M1 + M2 + M3 ARE ALL REGRESSION SURFACES.** Paint fill, camo break, movement/collision, the PWA build, the round loop, the projectile Tag, ticker, SPOTTED! stamp and reveal map are all signed off. **The Event view must render identically after M3 lands** — re-render and diff before shipping; flag any visual change to an approved surface, never ship it silently.

**M2 — what's in (all QA'd against real rendered output + canon-rule tests):**
- **Seeker bot** (`src/seeker.js`) — patrols by BFS path; sees UNHIDDEN hiders; a hidden hider only ripples when a mover comes close (the tell), and **sprinting past blinds you to it** (`sprintBlindness`). A ripple gives a *noisy* fix, so the seeker sometimes tags empty ground: **wrong tag −30 health, 4th mistake → spectator.** Found hiders **become seekers** (the 1v11 → 11v1 snowball); the pack's +15% speed boost fades as it grows.
- **AI dummy hiders** (`src/hider.js`) — 5 of them, running the player's exact camo rule, and obeying the camping rule: they camp, then make the daring run to a spot ≥3 tiles away to bank the bonus. That run is what the seeker gets to see.
- **Score ticker** (legible at a glance) — SCORE · a **coin that fades as the camp-decay eats your rate** · the **current REPAINT time** · HIDERS remaining · clock. Plus a **+100** flash on a banked reposition, and HIDE! / SEEKER RELEASED phase banners.
- **SPOTTED! stamp** on being found — display type, tilted 8°, Brawl Yellow on Paint Magenta (the locked motif) — then the **end-of-round reveal map** showing every hider's spot with SPOTTED!/SAFE chips, survival times, and a leaderboard. On desktop the summary docks beside the map so the reveal stays clean (it's the shareable artefact). **Play again** fully resets.
- **`CONCEPT` badge + Fan Content Policy notice** now in the UI and on the reveal panel (art hard-lines).
- **`?debug=1` extended** with round/score/seeker telemetry, per the standing rule.

**Two real bugs found by measuring, not guessing:** (1) dummy dwell counted down during the *hide* phase, so the whole pack broke cover on the exact frame the seeker released → mass finds in 4s; (2) `sprintBlindness` never engaged because the seeker's per-frame speed was **bimodal** (47% of frames read ~0 — it advanced a waypoint without moving), so the bot got full notice while actually sprinting. Fixed `AI.step` to spend its whole distance budget, and smoothed the speed estimate (EMA).

**🎯 THE TAG IS A FIRED PROJECTILE (build v6, Concept Brief v3.2 §104).** Tessa's M2 phone test found the design gap: when a seeker saw you, the round became a long outrun until they touched you. Wrong chase shape. The universal Tag is now a **single-target projectile**: range 3 tiles, travel speed **2× base move speed**, thin hitbox, **0.75s cooldown**, and **walls block it — so a spotted hider can play cover on the run.** Hitting any brawler, camouflaged *or* visibly running, is the find. A tag that hits **nothing, or a wall, is the wrong tag and costs the same 30.** Seekers commit to shots instead of spraying.
- Bot seekers fire it too, with **imperfect aim**: they **lead** a runner (badly enough that juking beats it) and take a **noisy fix** off a ripple, sharpening as they close but never to certainty. Once in range with a clear line they **stand and commit** — they never walk to point-blank (which would silently restore the touch-chase).
- **Measured, real numbers:** straight-line runner hit **88%** (running predictably is punished) · perfect reverse-juke hit **0%** (skill beats the lead) · ripple shot hit **87% @0.9 tiles / 78% @1.5 / 71% @2.5** — so every miss is a real 30. Two bugs caught by measuring: the bot walked to 0.31 tiles where a shot **cannot** miss (projectile spawns inside the hitbox), and its ripple fix collapsed to near-zero error at close range.

**⚠️ BALANCE — deliberately NOT re-tuned (Tessa's call, 07-07).** `TUNING.seeker.{noticeChance, rippleRadius, visionRadius, baseSpeed}` are **untouched**: the projectile tag changes chase dynamics fundamentally, so we ship it, Tessa re-tests on device, and only then do we touch those dials. For the record before the change, rounds ended in ~25–40s and the player was usually found before the clock (idle camping in the open centre gets found ~26–30s, which is *correct* — the anti-camping rule should punish that).

**📏 SCALED DEMO — map size and round length.** The prototype is a **scaled demo**: the arena is a compact 10×9 (the event spec borrows a Trio-Showdown canvas) and rounds run **15s hide + 60s seek = 75s** so every capture is clip-length. `TUNING.round.canonCapSeconds` keeps the event's **180s** cap as the real spec. Confirmed by Tessa 07-07-2026 — **and the case study describes it exactly this way** (Concept Brief v3.2 §106).

**CANON RULES VERIFIED** — 30 assertions, oracle = the spec written by hand, independent of the implementation. Tag: range ~3 tiles ✓ · speed exactly 2× base (7.40 tiles/s) ✓ · 0.75s cooldown, no second shot inside it ✓ · walls block it, hider behind cover survives ✓ · camouflaged hider hit = find ✓ · running hider hit = find ✓ · miss (air OR wall) = −30, 4th → spectator ✓. Round: score +10/s halving every 10s to a floor of 2/s ✓ · reposition ≥3 tiles banks +100 and resets the rate (and 1.5 tiles does **not**) ✓ · repaint 1.0 → 1.5 (3 found) → 2.0 (2 remain) ✓ · seeker 100hp, −30/wrong tag, 4th miss → spectator ✓ · 15s hide + 60s seek = **75s < 90s** ✓.

**M1 = REGRESSION SURFACE.** The paint fill, the camo break, movement/collision, and the PWA build are signed off. Re-render and re-check them before shipping any change that touches them; flag visual changes, never ship them silently.

**📱 HOW TO PLAY ON PHONE (recommended): install as a home-screen PWA.** Open the site in Safari → Share (□↑) → **Add to Home Screen** → launch from the icon. It runs fullscreen with no tab/URL bar, which is what removed Safari's own gestures (Safari tab-switched mid-round during testing). `manifest.webmanifest` + `apple-touch-icon.png` + iOS standalone meta are in place. **The landing page (Session 6) must document these install steps in its "play it" section.**

**🔎 `?debug=1` IS PERMANENT.** The overlay is a standing requirement in every future build (see CLAUDE.md). Extend it as systems land — the seeker, score and round state must be exposed there too. `?joystick=fixed` also stays.

**🎨 ART DIRECTION (FINAL 07-07-2026 — supersedes the earlier hybrid note; nothing to build now, just don't architect against it).** The art pass follows the **BrawlHouse workflow**: check the official **Supercell Fankit (fankit.supercell.com) FIRST for every asset** and use it **unmodified** where quality is up to par; only where official assets are **missing or below par**, generate **our own fan art of ACTUAL brawlers in Brawl Stars' art style** (recognisable characters) with **our own original backgrounds and arena tiles**. **Goal: the prototype reads as Brawl Stars art the way Elixir Hour reads as Clash Royale art.** Hard lines unchanged: **no extracted game files or sprite rips** (Fankit + our own art only); every build and capture keeps the *"unofficial / not endorsed by Supercell / Fan Content Policy"* notice **plus the `CONCEPT` label in the UI**. Renderer stays swap-friendly (`render.js drawBrawler`, `arena.js` tiles).

**⚖️ THE LESSON THAT COST TWO FAILED FIXES (now a standing rule in CLAUDE.md):** *verification oracles must be independent of the code under test.* A collision "escape sweep" decided which directions were free by calling the same buggy `collide()` it was testing — probe and actual agreed, it reported "0 stuck", and the bug shipped to Tessa's device twice. A passing sim is never sign-off; her device recording is the judge.

**🧪 RERUNNABLE PROOFS — `tools/` (committed 13-07).** The independent-oracle proofs now live in the repo, not a scratchpad: `python tools/run_all.py` runs **8 suites / 67 assertions** — collision (5,968-case grid-overlap oracle), round canon, projectile Tag, movement, speed/scale, **end-states (the four ways a round ends + the TAGGED OUT! edge cases)**, Map Maker live toggles + Event-view fit regression, and touch/UI. Uses system Chrome (`channel="chrome"`, no `playwright install`). The harness forces UTF-8 stdout so Unicode test names can't false-red the run on a Windows console. Any future session (and Session 3's capture rig) can confirm the build still honours the Concept Brief before touching it.

**MOVEMENT FIX (build v2, 07-07) — from Tessa's mobile playtest.** Two issues found and fixed, both verified against real output (a scripted collision+input escape sweep over every wall-adjacent tile × 8 directions, AND frames from a scripted phone-width play recording):
- **Stuck after hiding.** Two real causes: (a) a *crawl band* — drags under ~14px moved ≤8px/130ms (oversized 46px throw + analog curve); (b) a genuine *multi-touch lock* — when the hide-finger lifted while a second finger was down, the stick went dead until all fingers lifted (the "takes multiple tries" bug on iPad). **Fix (input.js):** robust multi-touch (first finger drives, strays ignored, hand-off to a remaining finger on release — no more dead-lock), a shorter 34px throw, and a response curve (MIN_K 0.5) that gives real speed straight out of a 4px dead-zone.
- **Rough/sticky free-walking.** Not tile-snapping (movement was already continuous) — the crawl curve plus no easing. **Fix (player.js):** velocity smoothing (fast ~40ms attack, quick release) with velocity zeroed into any blocked axis so it never fights a wall.
- **Result (measured in sim):** escape sweep 0 stuck / 0 clip across 296 (tile,dir) cases; multi-touch handoff works; regression green; zero console errors.

**⚠️ v2 DID NOT HOLD ON DEVICE — approach changed (build v3, 07-07).** Tessa's second iPad video (after a hard refresh on v2) showed the *same class* of fault: pinned at the right wall, stick deflected, barely moving. **The lesson: the scripted synthetic-touch sweep is NOT iOS Safari ground truth — it passed while the device failed.** So per Tessa's rule (two targeted fixes = no third reasoning-patch), the approach changed:
- **Instrument, don't guess.** A debug overlay (`?debug=1`, `src/debug.js`) draws the ground truth on screen: raw active touch points, stick anchor + current vector, intended vs applied velocity, which axes collision blocked this frame, plus the iOS gotchas — `preventDefault` success, `touch-action`, and `visualViewport` offset. **Verification is now Tessa's device recording with the overlay on, not the sim.**
- **iOS-Safari touch-path hardening** (`input.js` + `main.css`): touch listeners moved to the **#stage element** with `{passive:false}` (element-level = reliably non-passive, so preventDefault actually stops iOS pan/pinch/double-tap-zoom); input state **rebuilt from the authoritative `event.touches`** every event (kills stale-identifier bugs after multi-touch); `touch-action:none` + `overscroll-behavior:none` on the surface; page pinned `position:fixed` (no rubber-band); `visualViewport` resize/scroll re-fits the canvas when the Safari toolbar shows/hides mid-round. Movement math (throw/curve/smoothing) left UNCHANGED so the overlay isolates whether plumbing was the fault.
- **Fallback ready — fixed-anchor joystick** (`?joystick=fixed`): a standard fixed base bottom-left, generous 12px dead-zone, NO floating re-anchor (the floating anchor's drifting neutral point is the prime suspect). Boring, always works. To be adopted as default if the overlay shows input is the fault or v3 still doesn't hold.
- **Build v3 is sound in sim** (overlay renders, both stick modes drive movement, keyboard intact, zero console errors) — but **that proves nothing about iOS; awaiting Tessa's `?debug=1` device recording as ground truth.**

**✅ ROOT CAUSE FOUND — it was COLLISION, not input (build v4, 07-07).** Tessa's two `?debug=1` recordings were the ground truth: at the stuck moment the panel read **`applied v (0,0)`, `blocked X:YES Y:YES` while she pushed up-left into open space** — the collision was falsely blocking BOTH axes and eating the whole move, pinning her in a 1-tile-wide corridor. The fault survived a full input-scheme swap (float→fixed), exonerating input. **Why the earlier "0 stuck" sweeps lied: my "open direction" oracle was computed with the same buggy `collide()`, so probe and actual agreed — circular validation.** The bug is platform-independent geometry, so it always reproduced; the sim just never *checked* it correctly.
- **Fix 1 — collision rewritten as decoupled move-and-slide** (`arena.js`): X moves and resolves against the **original** y-band `[py±h]`; Y then moves and resolves against the **resolved** x-band. A blocked axis can never eat the free axis; nothing reverts the whole move. (The old code built the X-pass row band from `py+dy`, so a diagonal near a corner let one axis's wall poison the other.)
- **Fix 2 — repaint gated on engagement, not displacement** (`player.js` + `Input.engaged()`): pushing against a wall (displacement can be zero) can never start a hide; only a genuinely idle stick does.
- **PROOF (platform-independent, real this time):** a geometric sweep whose oracle is **pure grid overlap, independent of `collide()`** — 746 positions × 8 directions (5,968 cases): **0 clips, 0 stuck** (X or Y). Integration: corridor push-up-into-wall now **slides 57px, no clip**; holding into a wall 1.2s keeps `progress 0.00` (no repaint); idle still hides; break still works; zero console errors. This is geometry, so it holds on iOS too.
- **PWA added** (`manifest.webmanifest` + apple-touch-icon + iOS standalone meta): "Add to Home Screen" opens fullscreen — no Safari tab/URL bar or browser gestures (Safari tab-switched mid-round in Clip 2). **Recommended way to play on phone.** Icons generated (paint-drop + ripple).
- **Verification for sign-off stays device-first:** Tessa records from the home-screen icon showing wall-contact → slide → clean escape, and no repaint while the stick is deflected.

**ART DIRECTION (decided 07-07 for the later art pass — do NOT build now, just don't architect against it):** hybrid assets. (1) Official **Supercell Fankit** assets (fankit.supercell.com) used unmodified where they fit — reveal cards, end screens, marketing shell. (2) For in-game top-down sprites + arena tiles the kit won't cover, generate **our own Brawl-style fan art** (BrawlHouse method). **Two hard lines that never move:** every capture carries the required notice *"This material is unofficial and is not endorsed by Supercell. For more information see Supercell's Fan Content Policy: www.supercell.com/fan-content-policy"* **plus a small `CONCEPT` label in the game UI** (so nothing can pass as a real in-game screenshot); and **no extracted/ripped game files — Fankit + our own art only.** Keep the renderer swap-friendly: the brawler + tiles are drawn programmatically now (`render.js` `drawBrawler`, `arena.js` tiles) and must stay easy to replace with richer sprites without touching game logic.

**WHAT M1 PROVES (verified in real Chrome, not from numbers):**
- **The paint fill.** Stand still on/against a qualifying surface and, over `repaintTime` (1s default), a **magenta wet-paint edge sweeps left→right** across the brawler; behind the edge it repaints to the surface colour and fades to a hidden silhouette. A magenta progress ring + a "Painting in…" chip read the state. Fully hidden → the brawler all but vanishes ("strong, not perfect", `camoAlpha` floor) and a soft **teal you-are-here ripple** keeps the player oriented. Verified against wall-blend, water-blend and mid-sweep frames.
- **The camo break.** Any real movement instantly resets the fill and **flings a magenta paint-burst** off the body; name tag + health bar return. Verified live (keyboard-driven), not just posed.
- **Feels right on desktop AND phone.** Fit-contained arena; the sweep, ring, and vanish all read clearly at 390px. (Phone has generous vertical letterbox — a follow-camera could tighten it; deferred to M4.)
- **Live checks pass:** movement, wall collision (hard stop), the 1s timer → hidden, break-on-move → un-hidden, and the `repaint=0.5s` toggle (M3 pre-wire). **Zero console errors.**

**ARCHITECTURE (multi-file, data-driven — no colours/maps hard-coded in logic):**
- `src/config.js` — `CFG`: the locked v1.0 palette + gameplay tuning (repaintTime, speed, camoAlpha).
- `data/arena.js` — `ARENA`: the tile grid (10×9) + surface palette. Map geometry lives here only.
- `src/input.js` — `Input`: WASD/arrows + a floating touch thumb-stick → one normalised move vector.
- `src/arena.js` — `Arena`: grid parse, per-axis AABB collision, **camoSurface()** (wall > water > floor priority), and the chunky-Brawl tile render (beveled walls, checker floor, aqua pool).
- `src/fx.js` — `FX`: the paint break-burst, the camo-settle ring, the you-are-here pulse (all reduce-motion aware).
- `src/player.js` — `Player`: movement + the camo state machine (still → progress → hidden; move → break).
- `src/render.js` — `Render`: the paint-fill money shot (sweep clip, wet edge, ring, fading name tag/health).
- `src/game.js` — `STATE` + boot + fit-to-viewport transform + main loop + a **debug API** (`Game.pose/pause/skipHide/refit/newRound`) the capture rig uses to freeze exact frames. (Repaint is owned by `Round.repaintTime()` / `MAKER.repaint` — there is no `Game.setRepaint`.)

**BUILD STAMP RITUAL (every commit):** bump `CFG.BUILD.n` in `src/config.js` AND find-replace `?v=<old>`→`?v=<new>` across `index.html` (currently **`?v=13`**, BUILD n:13, milestone M4). The top-right title stamp (`v13 · M4`) is the witness — if it doesn't match, a script is cached stale.

**DEBUG / URL AFFORDANCES:** `?debug=1` (overlay now also shows view / surfaces / repaint / tell / **speed**), `?joystick=fixed` (fixed-anchor stick), `?view=maker` (opens the editor), **`?speed=<n>`** (global pace, 0–2, for on-device A/B). Combine freely.

**QA METHOD (reused every session):** Playwright driving **system Chrome via `channel="chrome"`** — no `playwright install` needed. `Game.pose({col,row,progress,facing})` freezes a deterministic frame; screenshot at exact device viewports; **read the real frames back and judge the picture, never the numbers.** Scripts live in the session scratchpad for M1; a permanent rig lands in `tools/` at the capture session (Session 3). Do **not** use the preview MCP's pixel screenshot (it hung at harness level on the Elixir Hour build).

**NEXT — Milestone 2 (after Tessa signs off M1):** one bot **seeker** patrolling; a few **AI dummy hiders**; the on-screen **score ticker** (survival + reposition bonus − camp decay); the **proximity ripple tell** when a mover passes a hidden brawler; **wrong-tag → SPOTTED! stamp**; **end-of-round reveal map** showing every hiding spot. A full round must run **under 90 seconds**.

**NEEDS TESSA:**
- **M1 sign-off gate.** Play it / watch the captures and confirm the paint fill + camo break "feel right" before M2 starts.
- Nothing else blocking. Original art is programmatic (a vector brawler + tile shapes) — no Supercell assets anywhere; a hero brawler illustration can replace the token later without touching logic.

**SOURCE OF TRUTH:** `../Concept Brief.md` (v2) wins on any design fact; `../Project Brief.md` is the process/visual master. This PLAN + `CLAUDE.md` track the build; the briefs win on conflict.

---

## M2 — canon tuning (Tessa, 07-07-2026; source: Concept Brief "Design tuning")

**These are canon, not placeholders.** They live in ONE block — `data/tuning.js` (`window.TUNING`) — for fast iteration. Never scatter them.

| System | Value |
|---|---|
| Hider score | **+10/s** while hidden; rate **halves every 10s** in the same spot (**floor 2/s**); shown as a **fading coin** on the ticker |
| Reposition bonus | repainting **≥3 tiles away** banks **+100** and **resets the rate** |
| Repaint time | **1.0s** at round start → **1.5s** once **three hiders found** → **2.0s** when **two remain**. Current value **always visible on the ticker** |
| Seeker | **100 health**; wrong tag **−30**; correct tags free (three mistakes survivable — **the fourth sends you to spectator**) |
| Round | **15s hide phase** before the seeker releases; **+15% seeker move speed while ≤2 seekers** (fades as the pack grows); **3-minute cap**; surviving hiders **win on the clock** |

**⚠️ One reconciliation, flagged not silently resolved:** canon says a **3-minute round cap**, but the Project Brief's acceptance criteria require **a full round under 90 seconds** so captures stay clip-length. The prototype therefore runs a **compressed round** (15s hide + 60s seek = **75s total**), with the canon 180s cap kept in `TUNING.round.canonCapSeconds` for reference. The design number is unchanged; only the prototype's demo round is compressed. Tessa to confirm at the M2 gate.

## Milestones (from the Session 2 kickoff)

- [x] **M0 — Scaffold.** Repo, folders, self-hosted fonts (Lilita One / Nunito Sans / Caveat), visual-system CSS tokens, `PLAN.md`, `CLAUDE.md`, `README.md`, `.gitignore`.
- [x] **M1 — Paint fill + camo break** feel right on desktop AND phone width. **✅ SIGNED OFF 10-07-2026** (Tessa, device-verified). Regression surface.
- [x] **M2 — Round loop.** Seeker bot, AI dummy hiders, score ticker, proximity-ripple tell, SPOTTED! stamp, reveal map. Round < 90s. **✅ SIGNED OFF 10-07-2026** (projectile Tag, v6). Regression surface.
- [x] **M3 — Map Maker view.** The same map in an editor frame with three live toggles: camo surfaces (walls/floor/water), repaint time (0.5/1/2s), ripple tell on/off. Flip a toggle → the mechanic changes live. **✅ SIGNED OFF 13-07-2026** (Tessa, device-verified). Regression surface.
- [x] **M4 — Polish.** Global speed baked 0.70 (Brawl-true), reduced-motion, performance, renderer swap-friendly + ART ASSET INVENTORY, mobile-controls polish (thumb-stick + landscape), and the **TAGGED OUT! fourth end state** (four end states, in-flight-tag resolution, seeker-budget HUD, 9-assertion sweep). **✅ SIGNED OFF 13-07-2026** (Tessa, device-verified on v13: *"The tagged out works perfectly."*). Regression surface. **← closes Session 2.**

## Log
- **07-07-2026** — Session 2 opens. Scaffolded the repo and shipped **M1 v1**: the one-second paint fill + camo break, QA'd at desktop + phone width. Public repo created + GitHub Pages. Then the mobile-fix saga from Tessa's iPad tests: **v2** (multi-touch lock + crawl + smoothing), **v3** (ground-truth `?debug=1` overlay + iOS touch-path hardening), **v4** (the real bug — decoupled move-and-slide collision — + PWA). The lesson: a sim sharing logic with the code under test is not a proof.
- **10-07-2026** — **M1 signed off** (device-verified); standing rules recorded (independent oracles, permanent `?debug=1`), final Fankit-first art direction, M2 canon tuning. Shipped **M2 v5** (seeker bot, score ticker, SPOTTED! stamp, end-of-round reveal map; round < 90s) and, after Tessa's M2 phone test, **v6 — the universal Tag became a fired projectile** (Concept Brief v3.2 §104: 2× speed, ~3-tile range, walls block, miss = −30). **M2 signed off.** Then built **M3 v7 — the Map Maker view**: the same arena in an editor frame with three live toggles (camo surfaces / repaint / ripple tell); found + fixed a `preventDefault` bug that had made every UI button dead on touch (incl. M2's Play again); removed the lying `Game.setRepaint`.
- **13-07-2026** — **M3 passed an independent PM review.** Shipped **v8** (doc + hardening): reconciled the UI-touch set per `touchstart` so a dropped iOS touchend + reused id can't ghost the stick; committed the rerunnable proof suite under **`tools/`** (6 suites / 43 assertions, independent oracles); corrected the debug-API doc line and the stale dates; noted the Floor toggle's ~8% no-camo characteristic (working as intended). Held at the M3 gate for Tessa's recording.
- **13-07-2026** — **M4 polish** (v9–v11): baked global speed to **`speedScale = 0.70`** (Tessa's device A/B pick, and Brawl-true — player ≈ Very Fast, seeker ≈ Normal); demoted the in-game pace picker to a `?debug`/`?pace` affordance; full `prefers-reduced-motion` pass; performance pass (60fps under throttle); confirmed the renderer stays sprite-swap-friendly; delivered the expanded **Art Inventory** handoff. Scoring-at-release (v3.3) locked in on the ticker.
- **13-07-2026** — **M4 reopened & shipped v12 — the fourth end state.** Tessa's pace test exposed a dead-round hole (kite every seeker to spectator → timeout with zero threat, score still ticking). Added the **fourth end state** (all seekers benched + no tag in flight → hiders win), with the end check running **after** tag resolution so an in-flight tag resolves first (a hit converts a hider to a fresh 100hp seeker and the round continues). Added the seeker-budget HUD (SEEKERS chip + per-seeker mistake pips/health bar), per-end-state reveal copy + a teal stamp, and `tools/test_endstates.py` (9 independent-oracle assertions). Full suite now **8 suites / 67 assertions**, all green.
- **13-07-2026** — **v13 — renamed the end state to "TAGGED OUT!"** (Tessa: "SEEKERS EXHAUSTED!" wasn't Brawl enough; TAGGED OUT! turns the Tag mechanic back on the seekers and pairs with SPOTTED! as a stamp family). Renamed across banner/reveal/stamp and the internal reason id (`'tagged-out'`), bumped the stamp threshold so the stamp family stays full-size, re-ran the suite green, re-captured the stamp + reveal at desktop and phone. Also locked the **Fresh Paint** roster into `../Art Inventory.md` (Leon = player; Shelly/Nita/El Primo/Spike/Dynamike; **Belle** = seeker).
- **13-07-2026** — **✅ M4 SIGNED OFF on v13 — SESSION 2 COMPLETE.** Tessa's device recording confirmed TAGGED OUT! frame by frame (*"The tagged out works perfectly."*): the reveal card, 0/6 found, all-SAFE map, correct leaderboard; seeker health pips + SEEKERS chip mid-round; pace and release-gated scoring both behaving; the immediate end firing at 0:20 right after the last tag was spent. All four M-gates are now signed off; the v13 Event + Map Maker build is the frozen regression baseline. **Next: Session 2b (art pass).**
- **📝 For the case study to mine (13-07-2026):** three of this build's best results came straight out of the **stop-and-show-me gate discipline**, not from planning them upfront. (1) The **projectile Tag** — M2's "chase becomes a long outrun" only showed up on Tessa's *device*, and reframing the universal Tag as a fired projectile fixed the whole chase shape. (2) The **pace convergence** — a blind on-device A/B landed on 0.70, and *then* research showed 0.70 is Brawl-true (player ≈ Very Fast/Leon's class, hider:seeker ratio 0.881 ≈ Brawl's 0.878): feel and data agreed independently. (3) The **TAGGED OUT! clip shape** — a genuine design hole (kite every seeker → dead round) surfaced only in a real pace test, and closing it *added* a shareable win-state clip. The throughline: **the device is the judge; the gates turned playtest friction into the design.**
